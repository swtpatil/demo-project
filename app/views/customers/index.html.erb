<% if customer_signed_in? %> 
 <div> Welcome <%= current_customer.email %> </div> 
  <%= button_to "Sign out", destroy_customer_session_path, method: :delete %> 
<% else %>
  <%= button_to "Sign in", new_customer_session_path %> 
<% end %>

<h1> Custormers List </h1>

<ul>
  <% @customers.each do |customer| %>
    <li>
      <%= customer.name %>
    </li>
  <% end %>
</ul>


